import { StyleSheet } from "react-native";


export const styleBtn = StyleSheet.create({
    btnMeal: {
        display: 'flex',
        width: '100%',
        height: 'auto',
        padding: 16,
        margin: 'auto',
    }
})